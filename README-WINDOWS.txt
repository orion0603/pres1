================================
SIMPLE WINDOWS INSTRUCTIONS
================================

Copy and paste this SINGLE command into PowerShell:

cd client && npm install && npx vite

That's it! Once it's running, open:
http://localhost:5173 in your browser.

================================